import { Placeholder } from "./Placeholder";

export function Agents() {
  return (
    <Placeholder
      title="Mini-agentes"
      subtitle="cron · event · manual"
      description="Cron jobs inteligentes (sonarr-watcher, grid-monitor, email-watcher). Reemplazan los .sh + cron huérfanos. Reportan al user, al main o a un topic."
      accent="orange"
    />
  );
}
